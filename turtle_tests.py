import turtle

t = turtle.Pen()
# turtle.exitonclick()
t.forward(50)
t.left(90)
t.forward(50)
t.left(90)

# t.reset() erases the canvas and resets the turtle to starting position
# t.clear() clears the screen and leaves the turtle where it is
# t.backward(90)
# t.up() lift the pen, stop drawing
# t.down() start drawing


turtle.done()
